-> xor bit a bit e expansão de "chave" (aka o padrão? passwd? expandido)
-> s-box é gerada à cabeça, verifica se se o maior valor está lá, se não estiver
mete se no fundo e dá se shuffle com o (padrão? passwd? )
-> stack com operações?
-> somar o valor em X com o valor em -X até metade do array? (e uma operação para ser ao contrário)
-> shuffle 
-> ter lista de primos que serão usados para contas para cada operação? 
com a dita cuja a ser aumentada de cada vez

-> ter lista de shufflers e lista de transformadores, sendo que corre 1 shuffler
e 1 transformador para cada posição ser mudada de sítio e ser usada para op 
diferente com a seed